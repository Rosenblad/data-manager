{
  "name": "@data-manager/devutils",
  "version": "1.0.0",
  "description": "",
  "main": "./src/index.ts",
  "bin": {
    "dm-watcher": "./lib/watcher.js"
  },
  "scripts": {
    "clean": "rm -rf lib ./*.tgz",
    "compile": "npm run clean && tsc --declaration",
    "pack": "npm run compile && npm pack",
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "npm run compile && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "12.6.8",
    "ts-node": "8.3.0",
    "typescript": "3.5.3"
  },
  "dependencies": {
    "@testing-library/react": "8.0.7",
    "chalk": "2.4.2",
    "jest": "24.8.0",
    "ts-jest": "24.0.2"
  }
}
